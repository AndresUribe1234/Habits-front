import LoginForm from "@/components/auth/LoginForm";
import React from "react";

const Login = (props) => {
  console.log(props);
  return (
    <React.Fragment>
      <LoginForm />
    </React.Fragment>
  );
};

export default Login;
